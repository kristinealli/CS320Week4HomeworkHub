---
- name: restart httpd
  ansible.builtin.service:
    name: "{{ httpd_service }}"
    state: restarted
    enabled: true

- name: restart php-fpm
  ansible.builtin.service:
    name: "{{ php_service }}"
    state: restarted
    enabled: true
